"""Summarize the six-paper benchmark and source-reviewed table cell anchors."""
import json,sys
from pathlib import Path
sys.path.insert(0,'docling-lab');from cloud_benchmark import OUT,CASES,save
rows=[json.loads(p.read_text()) for p in OUT.glob('*/*/measurement.json')]
lookup={(r['provider'],r['case']):r for r in rows}
# Source-reviewed row/column values from scikit-learn p4; compare coordinates, not a bag of numbers.
gold=[['5.2','9.47','17.5','11.52','40.48','5.63'],['1.17','105.3','-','37.35','-','-'],['0.52','73.7','-','1.44','-','-'],['0.57','1.41','-','0.56','0.58','1.36'],['0.18','-','-','8.93','0.47','0.33'],['1.34','0.79','⋆','-','35.75','0.68']]
audits=[]
for provider in ['azure','docling']:
 for case in ['scikit-learn','scikit-learn-scan']:
  out=OUT/provider/case
  r=json.loads((out/('response.json' if provider=='azure' else 'raw.json')).read_text())
  tables=r['analyzeResult']['tables'] if provider=='azure' else r['tables']
  t=next(t for t in tables if (t['boundingRegions'][0]['pageNumber'] if provider=='azure' else t['prov'][0]['page_no'])==4)
  cells={(c['rowIndex'],c['columnIndex']):c['content'] for c in t['cells']} if provider=='azure' else {(c['start_row_offset_idx'],c['start_col_offset_idx']):c['text'] for c in t['data']['table_cells']}
  mismatches=[]
  for i,row in enumerate(gold,1):
   for j,value in enumerate(row,1):
    actual=cells.get((i,j),'').strip().replace('★','⋆').replace('*','⋆')
    if actual!=value:mismatches.append({'row':i,'column':j,'expected':value,'actual':actual})
  audits.append(dict(provider=provider,case=case,matched=36-len(mismatches),total=36,mismatches=mismatches))
save(OUT/'table-cell-audit.json',audits);save(OUT/'summary.json',rows)
lines=['# Cloud OCR comparison — 2026-09-08','','Azure Document Intelligence is a promising candidate for a Rpaper integration trial. It was faster in this six-document experiment and retained more source tokens on each document. It is not an exact replacement for the current pipeline. Mistral has not been tested: no API key was available.','','## Measured results','','Identical PDFs (SHA-256 recorded per run), 61 pages total. Docling ran locally on macOS, CPU with two threads, through the current `app_parse.py` including export and process startup. Azure ran in Central India, `prebuilt-layout`, API 2024-11-30, Markdown + figures + formula add-on. This is a deployment-option comparison, not a controlled hardware benchmark. Neither column includes the production queue delay. Azure timing includes upload and polling (two-second interval), but excludes subsequent figure downloads and Rpaper manifest conversion. One run per document; no p95 claim.','','| PDF | Pages | Docling seconds | Azure seconds | Docling token overlap | Azure token overlap |','|---|---:|---:|---:|---:|---:|']
for c in CASES:
 d=lookup['docling',c];a=lookup['azure',c]
 lines.append(f"| {c} | {d['metrics']['pages']} | {d['wall_seconds']:.2f} | {a['wall_seconds']:.2f} | {d['metrics']['token_recall_proxy']:.1%} | {a['metrics']['token_recall_proxy']:.1%} |")
ds=sum(lookup['docling',c]['wall_seconds'] for c in CASES);azs=sum(lookup['azure',c]['wall_seconds'] for c in CASES)
lines += ['',f'Total measured wall time: Docling {ds:.2f}s; Azure {azs:.2f}s ({ds/azs:.1f}x lower for this experiment, subject to the timing differences above).','','Token overlap is multiset recall against native PDF text, using the digital original for the scan. It is not an accuracy score: it does not measure extra invented text, reading order, equation meaning, typography, or cell placement. PDF extraction itself is an imperfect reference.','','## Quality checks','','Both parsers returned every page and matched all 83 selected heading-title anchors in order. Heading depth and complete multi-column reading order are not established by these checks.','']
for x in audits: lines.append(f"- {x['provider']} / {x['case']}: {x['matched']}/{x['total']} source-reviewed data cells matched at the expected row and column on page 4.")
lines += ['','- The scanned Docling table missed ten dash markers (unavailable-operation cells); all 26 other checked cells matched. Its table audit also flags these unassigned slots, so this is not a claim that numeric values were wrong.', '- Azure classified the scikit-learn author/contact block on page 1 as an extra table, in both the digital and scanned PDF. Its table count is 2 versus the single actual benchmark table.','- Azure returned formula placeholders in the structured cells of Attention table 1. Formula objects/positions must be resolved by our adapter; copying cell text directly would lose the mathematical expressions.','- Visual comparison of Attention page 6 found Azure transcribed the sine equation’s `d_model` subscript as `d_madel`. The cosine equation kept `model`. Formula recognition is useful but not exact. Our current Docling app runs with formula enrichment disabled; its formula-region count must not be compared as a recognition score.','- Azure’s downloaded Transformer architecture figure crop was visually intact. Composite figures can be split: counts are not a quality verdict. All returned figure files were downloaded; exhaustive image/caption review remains open.','','## Cost','','The Azure Retail Prices API returned Central India S0 rates of $10/1,000 prebuilt pages plus $6/1,000 add-on pages. At one formula add-on per page, 61 pages implies about **$0.976** in analysis charges, before taxes/other usage. This is a retail estimate, not a verified invoice or credit deduction. Saved meters: `tmp/cloud-ocr-benchmark/azure-prices.json`.','', 'Mistral’s current published OCR price is $4/1,000 pages: the same 61-page run would be about $0.244 at that rate, but no request was made and no quality or latency result is available.','','## Recommendation','','Complete the Mistral run before selecting a provider. Azure merits a small adapter trial with the current reader format, including formula resolution, figure/caption relationships, source coordinates, table validation, and preservation of source crops for uncertain equations. Keep the original PDF accessible. This benchmark does not authorize or perform a production parser switch.','','The resource `rpaper-ocr-benchmark` (Document Intelligence S0, Central India) was created in `rpaper-staging`; it is a per-use service, not a persistent parser VM. No app revision, production document, database schema, or queue setting was changed. API keys were kept in process memory. Public research fixtures only were sent to Azure.','','## Reproduce','','```sh','docling-lab/.venv/bin/python docling-lab/cloud_benchmark.py docling','docling-lab/.venv/bin/python docling-lab/cloud_benchmark.py azure','# Add MISTRAL_API_KEY to ignored .env.local first:','docling-lab/.venv/bin/python docling-lab/cloud_benchmark.py mistral','docling-lab/.venv/bin/python docling-lab/cloud_benchmark_report.py','```','','Successful runs are reused by input hash; choose a fresh output directory in the script before intentional repeated timing runs. Raw JSON, Markdown, measurements, source review PNGs, figure crops, and table audits are under `tmp/cloud-ocr-benchmark/`. Provider output is untrusted and has not been inserted into the app.','','## Sources','','- [Azure layout model](https://learn.microsoft.com/en-us/azure/ai-services/document-intelligence/prebuilt/layout?view=doc-intel-4.0.0)','- [Azure analysis REST API](https://learn.microsoft.com/en-us/rest/api/aiservices/document-models/analyze-document?view=rest-aiservices-v4.0%20(2024-11-30))','- [Azure pricing](https://azure.microsoft.com/en-us/pricing/details/document-intelligence/)','- [Azure Retail Prices API](https://prices.azure.com/api/retail/prices)','- [Mistral OCR API](https://docs.mistral.ai/studio/document-processing/basic_ocr)','- [Mistral pricing](https://mistral.ai/pricing/api/)','']
# Derive anchor total rather than trusting a manually typed total.
count=sum(lookup['azure',c]['metrics']['heading_anchors_total'] for c in CASES)
lines=[s.replace('all 83 selected',f'all {count} selected') for s in lines]
Path('docs/CLOUD_OCR_BENCHMARK.md').write_text('\n'.join(lines))
print({'docling_seconds':ds,'azure_seconds':azs,'heading_anchors':count,'table_audits':audits})
